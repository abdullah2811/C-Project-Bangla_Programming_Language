সংযোগ <সটডিও>
সংযোগ <গণিত>

পূর্ণ যোগফল(পূর্ণ x, পূর্ণ y) {
    ফেরাও x + y;
}

প্রধান() {
    পূর্ণ সংখ্যা১, সংখ্যা২, ফলাফল;

    লেখো("প্রথম সংখ্যা দাও: ");
    নাও("%d", &সংখ্যা১);

    লেখো("দ্বিতীয় সংখ্যা দাও: ");
    নাও("%d", &সংখ্যা২);

    ফলাফল = যোগফল(সংখ্যা১, সংখ্যা২);

    যদি (ফলাফল > 100) {
        লেখো("ফলাফল অনেক বড়!\n");
    } নাহলে {
        লেখো("ফলাফল: %d\n", ফলাফল);
    }

    // একটি সাধারণ for লুপ
    জন্য (পূর্ণ i = 0; i < ৫; i++) {
        লেখো("i এর মান: %d\n", i);
    }

    ফেরাও 0;
}
